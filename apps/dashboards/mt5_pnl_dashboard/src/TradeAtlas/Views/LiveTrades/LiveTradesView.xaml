<UserControl x:Class="TradeAtlas.Views.LiveTrades.LiveTradesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:TradeAtlas.ViewModels"
             xmlns:conv="clr-namespace:TradeAtlas.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:LiveTradesViewModel, IsDesignTimeCreatable=False}">

    <UserControl.Resources>
        <conv:PnlToColorConverter x:Key="PnlToColorConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ToolBar Grid.Row="0">
            <Button Content="Connect" Command="{Binding ConnectCommand}" Margin="2" />
            <Button Content="Disconnect" Command="{Binding DisconnectCommand}" Margin="2" />
            <Separator />
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Margin="2" />
            <Separator />
            <TextBlock VerticalAlignment="Center" Margin="8,0">
                <Run Text="Status: " />
                <Run Text="{Binding IsConnected, Mode=OneWay}" Foreground="Green" />
            </TextBlock>
        </ToolBar>

        <!-- Open Positions Grid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding OpenPositions}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  Margin="4">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ticket" Binding="{Binding Ticket}" Width="80" />
                <DataGridTextColumn Header="Symbol" Binding="{Binding Symbol}" Width="80" />
                <DataGridTextColumn Header="Direction" Binding="{Binding Direction}" Width="60" />
                <DataGridTextColumn Header="Volume" Binding="{Binding Volume, StringFormat='{}{0:F2}'}" Width="60" />
                <DataGridTextColumn Header="Open Price" Binding="{Binding OpenPrice, StringFormat='{}{0:F5}'}" Width="100" />
                <DataGridTextColumn Header="Current" Binding="{Binding CurrentPrice, StringFormat='{}{0:F5}'}" Width="100" />
                <DataGridTextColumn Header="Unrealized P&amp;L" Binding="{Binding UnrealizedPnl, StringFormat='{}{0:F2}'}" Width="100" />
                <DataGridTextColumn Header="SL" Binding="{Binding StopLoss, StringFormat='{}{0:F5}'}" Width="80" />
                <DataGridTextColumn Header="TP" Binding="{Binding TakeProfit, StringFormat='{}{0:F5}'}" Width="80" />
                <DataGridTextColumn Header="Magic" Binding="{Binding MagicNumber}" Width="80" />
                <DataGridTextColumn Header="Comment" Binding="{Binding OrderComment}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Summary Bar -->
        <Border Grid.Row="2" BorderBrush="LightGray" BorderThickness="0,1,0,0" Padding="8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Total Unrealized P&amp;L: " FontWeight="Bold" VerticalAlignment="Center" />
                <TextBlock Text="{Binding TotalUnrealizedPnl, StringFormat='{}{0:F2}'}"
                           FontSize="16" FontWeight="Bold" VerticalAlignment="Center" Margin="4,0"
                           Foreground="{Binding TotalUnrealizedPnl, Converter={StaticResource PnlToColorConverter}}" />
                <TextBlock Text="  |  Positions: " VerticalAlignment="Center" Margin="16,0,0,0" />
                <TextBlock Text="{Binding OpenPositions.Count}" FontWeight="Bold" VerticalAlignment="Center" Margin="4,0" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

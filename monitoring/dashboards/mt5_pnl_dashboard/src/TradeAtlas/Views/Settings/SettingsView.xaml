<UserControl x:Class="TradeAtlas.Views.Settings.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:TradeAtlas.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel, IsDesignTimeCreatable=False}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Settings" FontSize="20" FontWeight="Bold" Margin="0,0,0,16" />

        <StackPanel Grid.Row="1" MaxWidth="500" HorizontalAlignment="Left">
            <!-- Database Path -->
            <TextBlock Text="Database Path" FontWeight="SemiBold" Margin="0,0,0,4" />
            <TextBox Text="{Binding DatabasePath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

            <!-- Theme -->
            <TextBlock Text="Theme" FontWeight="SemiBold" Margin="0,0,0,4" />
            <ComboBox SelectedItem="{Binding ThemeName}" Margin="0,0,0,12">
                <ComboBoxItem Content="Light" />
                <ComboBoxItem Content="Dark" />
            </ComboBox>

            <!-- SMTP Host -->
            <TextBlock Text="SMTP Host" FontWeight="SemiBold" Margin="0,0,0,4" />
            <TextBox Text="{Binding SmtpHost, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

            <!-- SMTP Port -->
            <TextBlock Text="SMTP Port" FontWeight="SemiBold" Margin="0,0,0,4" />
            <TextBox Text="{Binding SmtpPort, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />

            <!-- Save Button -->
            <Button Content="Save Settings" Command="{Binding SaveSettingsCommand}"
                    Padding="16,8" HorizontalAlignment="Left" Margin="0,8,0,0" />
        </StackPanel>
    </Grid>
</UserControl>

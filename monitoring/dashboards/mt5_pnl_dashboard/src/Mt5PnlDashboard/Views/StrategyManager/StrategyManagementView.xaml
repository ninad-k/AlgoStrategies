<UserControl x:Class="Mt5PnlDashboard.Views.StrategyManager.StrategyManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Mt5PnlDashboard.ViewModels"
             xmlns:conv="clr-namespace:Mt5PnlDashboard.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:StrategyManagementViewModel, IsDesignTimeCreatable=False}"
             Loaded="OnLoaded">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ToolBar Grid.Row="0">
            <Button Content="Add Strategy" Command="{Binding AddStrategyCommand}" Margin="2" />
            <Button Content="Edit" Command="{Binding EditStrategyCommand}" Margin="2" />
            <Button Content="Delete" Command="{Binding DeleteStrategyCommand}" Margin="2" />
            <Separator />
            <Button Content="Auto-Detect" Command="{Binding AutoDetectCommand}" Margin="2" />
            <Button Content="Refresh" Command="{Binding LoadStrategiesCommand}" Margin="2" />
        </ToolBar>

        <!-- Strategies Grid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Strategies}"
                  SelectedItem="{Binding SelectedStrategy}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  Margin="4" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="200" />
                <DataGridTextColumn Header="Magic Numbers" Binding="{Binding MagicNumbers}" Width="120" />
                <DataGridTextColumn Header="Symbol Filter" Binding="{Binding SymbolFilter}" Width="100" />
                <DataGridTextColumn Header="Comment Pattern" Binding="{Binding CommentPattern}" Width="120" />
                <DataGridTextColumn Header="Lot Min" Binding="{Binding LotSizeMin, StringFormat='{}{0:F2}'}" Width="60" />
                <DataGridTextColumn Header="Lot Max" Binding="{Binding LotSizeMax, StringFormat='{}{0:F2}'}" Width="60" />
                <DataGridCheckBoxColumn Header="Auto" Binding="{Binding IsAutoDetected}" Width="50" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Detail Panel -->
        <Border Grid.Row="2" BorderBrush="LightGray" BorderThickness="0,1,0,0" Padding="8" MinHeight="80">
            <StackPanel Orientation="Horizontal" DataContext="{Binding SelectedStrategy}">
                <TextBlock Text="Selected: " FontWeight="Bold" VerticalAlignment="Center" />
                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Margin="4,0" />
                <TextBlock Text="{Binding Description}" VerticalAlignment="Center" Margin="16,0" Foreground="Gray" />
            </StackPanel>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="3" Background="#80FFFFFF"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="Loading..." FontSize="18"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>
    </Grid>
</UserControl>

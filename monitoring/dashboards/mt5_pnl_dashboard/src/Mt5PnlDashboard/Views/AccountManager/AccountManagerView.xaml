<UserControl x:Class="Mt5PnlDashboard.Views.AccountManager.AccountManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Mt5PnlDashboard.ViewModels"
             xmlns:conv="clr-namespace:Mt5PnlDashboard.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:AccountManagerViewModel, IsDesignTimeCreatable=False}"
             Loaded="OnLoaded">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ToolBar Grid.Row="0">
            <Button Content="Add Account" Command="{Binding AddAccountCommand}" Margin="2" />
            <Button Content="Edit" Command="{Binding EditAccountCommand}" Margin="2" />
            <Button Content="Delete" Command="{Binding DeleteAccountCommand}" Margin="2" />
            <Separator />
            <Button Content="Sync" Command="{Binding SyncAccountCommand}" Margin="2" />
            <Button Content="Refresh" Command="{Binding LoadAccountsCommand}" Margin="2" />
        </ToolBar>

        <!-- Accounts Grid -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Accounts}"
                  SelectedItem="{Binding SelectedAccount}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  Margin="4" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="MT5 Login" Binding="{Binding Mt5Login}" Width="100" />
                <DataGridTextColumn Header="Server" Binding="{Binding Server}" Width="150" />
                <DataGridTextColumn Header="Primary Trader" Binding="{Binding PrimaryTraderName}" Width="120" />
                <DataGridTextColumn Header="Type" Binding="{Binding AccountType}" Width="80" />
                <DataGridTextColumn Header="Balance" Binding="{Binding Balance, StringFormat='{}{0:F2}'}" Width="100" />
                <DataGridTextColumn Header="Equity" Binding="{Binding Equity, StringFormat='{}{0:F2}'}" Width="100" />
                <DataGridTextColumn Header="Currency" Binding="{Binding Currency}" Width="60" />
                <DataGridTextColumn Header="Last Sync" Binding="{Binding LastSyncAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="140" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Detail Panel -->
        <Border Grid.Row="2" BorderBrush="LightGray" BorderThickness="0,1,0,0" Padding="8" MinHeight="80">
            <StackPanel Orientation="Horizontal" DataContext="{Binding SelectedAccount}">
                <TextBlock Text="Selected: " FontWeight="Bold" VerticalAlignment="Center" />
                <TextBlock Text="{Binding Mt5Login}" VerticalAlignment="Center" Margin="4,0" />
                <TextBlock Text="{Binding Server}" VerticalAlignment="Center" Margin="4,0" Foreground="Gray" />
                <TextBlock Text="{Binding StrategiesCsv}" VerticalAlignment="Center" Margin="16,0" Foreground="DarkBlue" />
            </StackPanel>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="3" Background="#80FFFFFF"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="Loading..." FontSize="18"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>
    </Grid>
</UserControl>

<UserControl x:Class="Mt5PnlDashboard.Views.UncategorizedTrades.UncategorizedTradesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:Mt5PnlDashboard.ViewModels"
             xmlns:conv="clr-namespace:Mt5PnlDashboard.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:UncategorizedTradesViewModel, IsDesignTimeCreatable=False}"
             Loaded="OnLoaded">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ToolBar Grid.Row="0">
            <Button Content="Assign Manual" Command="{Binding AssignManualCommand}" Margin="2" />
            <Button Content="Assign Auto" Command="{Binding AssignAutoCommand}" Margin="2" />
            <Button Content="Auto-Classify All" Command="{Binding AutoClassifyCommand}" Margin="2" />
            <Separator />
            <Button Content="Refresh" Command="{Binding LoadUncategorizedCommand}" Margin="2" />
        </ToolBar>

        <!-- Trades Grid with Checkbox -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Trades}"
                  AutoGenerateColumns="False" IsReadOnly="False"
                  Margin="4" SelectionMode="Extended">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="" Width="30" />
                <DataGridTextColumn Header="Ticket" Binding="{Binding Ticket}" Width="80" IsReadOnly="True" />
                <DataGridTextColumn Header="Symbol" Binding="{Binding Symbol}" Width="80" IsReadOnly="True" />
                <DataGridTextColumn Header="Direction" Binding="{Binding Direction}" Width="60" IsReadOnly="True" />
                <DataGridTextColumn Header="Volume" Binding="{Binding Volume, StringFormat='{}{0:F2}'}" Width="60" IsReadOnly="True" />
                <DataGridTextColumn Header="Magic" Binding="{Binding MagicNumber}" Width="80" IsReadOnly="True" />
                <DataGridTextColumn Header="Comment" Binding="{Binding OrderComment}" Width="150" IsReadOnly="True" />
                <DataGridTextColumn Header="Entry Time" Binding="{Binding EntryTime, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="130" IsReadOnly="True" />
                <DataGridTextColumn Header="P&amp;L" Binding="{Binding ProfitLoss, StringFormat='{}{0:F2}'}" Width="80" IsReadOnly="True" />
                <DataGridTextColumn Header="Origin" Binding="{Binding TradeOrigin}" Width="80" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- Bulk Action Bar -->
        <Border Grid.Row="2" BorderBrush="LightGray" BorderThickness="0,1,0,0" Padding="8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Total uncategorized: " VerticalAlignment="Center" />
                <TextBlock Text="{Binding Trades.Count}" FontWeight="Bold" VerticalAlignment="Center" Margin="4,0" />
            </StackPanel>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="3" Background="#80FFFFFF"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <TextBlock Text="Classifying..." FontSize="18"
                       HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>
    </Grid>
</UserControl>
